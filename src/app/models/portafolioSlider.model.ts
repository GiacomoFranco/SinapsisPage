export interface SliderPortafolio {
    slug: string;
    general: {
        logo: string;
    }
}

export interface infoPortafolio {
    titulo_casos_de_exito: string;
    descripcion_casos_de_exito: string;
}